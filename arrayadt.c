#include<stdio.h>
#include<stdlib.h>
struct Arrayadt{
  int capacity;
  int size;
  int * aptr;
};
int main(){
  struct Arrayadt arr;
  int capacity,size;
  printf("Enter the capacity of the array");
  scanf("%d",capacity);
  printf("Enter the size of the array");
  scanf("%d",size);


}