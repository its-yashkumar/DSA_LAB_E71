#include<stdio.h>
#include<stdlib.h>
struct Node{
  int data;
  struct Node * next;
};
struct Node * head,
void createlinkedlist(struct Node *);
int main(){
    
}
void createlinkedlist(struct Node *){
    head=(struct Node *)malloc(sizeof(struct Node));
}